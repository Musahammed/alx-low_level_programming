#include "main.h"

/**
 * print_times_table - prints the n times table, starting with
 * @n: time table to print
 */
void print_times_table(int n)
{
int ii, jj, mul;

if (n < 0 || n > 15)
return;
for (ii = 0; ii <= n; ii++)for (jj = 0; jj <= n; j
