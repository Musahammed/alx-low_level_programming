#include "main.h"

/**
 * factorial - factorial of a number
 * @n: integer
 * Return: integer
 */
int factorial(int n)
{
if (i == 0)
return (0);
{
if (i == 1)
return(i);
{
else
return (factorial(n - 1) + facttorial(n + 1));
}
_putchar('\n');
}
}

