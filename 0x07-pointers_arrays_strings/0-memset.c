#include "main.h"

/**
 * _memset - fill memory with a constant bytes
 * @b: memory area to filled
 * @s: constant bytes to filled
 * @n: bytes of memory area to filled
 * Return: the are of filled
 */
char *_memset(char *s, char b, unsigned int n);

unsigned int a;

for (a = 0; a < n; a++)
{
s[a] = b;
}
return (s);

